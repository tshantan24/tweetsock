// var loading = {
// 	container: document.getElementById('load'),
// 	renderer: 'svg',
// 	loop: true,
// 	autoplay: true,
// 	animationData: animationData_loading
// };

// var anim3;

// anim3 = lottie.loadAnimation(loading); //This is for the loading animation, use when its retrieving tweets

var confetti = {
	container: document.getElementById('confetti'),
	renderer: 'svg',
	loop: true,
	autoplay: true,
	animationData: animationData_confetti
};

var anim2;

anim2 = lottie.loadAnimation(confetti);

var republican = {
	container: document.getElementById('rep'),
	renderer: 'svg',
	loop: true,
	autoplay: true,
	animationData: animationData_repo
};

var anim;

anim = lottie.loadAnimation(republican);


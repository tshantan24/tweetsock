var confetti = {
	container: document.getElementById('confetti'),
	renderer: 'svg',
	loop: true,
	autoplay: true,
	animationData: confe
};

var anim;

anim = lottie.loadAnimation(confetti);

var confetti = {
	container: document.getElementById('confetti-2'),
	renderer: 'svg',
	loop: true,
	autoplay: true,
	animationData: confe
};

var anim;

anim = lottie.loadAnimation(confetti);